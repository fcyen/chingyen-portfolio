name: Refresh Substack feed
on:
  schedule:
    - cron: "0 6 * * *"   # 6 AM UTC daily

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify build
        run: curl -s -X POST "${{ secrets.NETLIFY_BUILD_HOOK }}"
